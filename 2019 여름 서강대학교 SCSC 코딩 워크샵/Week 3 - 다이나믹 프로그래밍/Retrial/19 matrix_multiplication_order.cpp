#include <iostream>

int mat[500][2];
int dp[500][500];

int main() {
	std::cin.tie(0);
	std::cout.tie(0);
	std::ios::sync_with_stdio(0);

	int N;
	std::cin >> N;

	for (int i = 0; i < N; i++) std::cin >> mat[i][0] >> mat[i][1];

	

	for (int i = 1; i < N; i++) {
		
	}
}